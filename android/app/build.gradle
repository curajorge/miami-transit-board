plugins {
    id "com.android.application"
}

android {
    namespace "com.jcm.miamitrolleylive"
    compileSdk 35

    defaultConfig {
        applicationId "com.jcm.miamitrolleylive"
        minSdk 26
        targetSdk 35
        versionCode 2
        versionName "0.2.0"
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    sourceSets.main.assets.srcDir(layout.buildDirectory.dir("generated/webAssets"))
}

dependencies {}

tasks.register("prepareWebAssets", Sync) {
    from("../..") {
        include "index.html", "styles.css", "engine.js", "app.js", "go.js", "go.css", "bus-stops.js", "trolley-stops.js", "vendor/**", "assets/brand/miami-transit-after-hours.png"
    }
    into(layout.buildDirectory.dir("generated/webAssets/web"))
}

tasks.named("preBuild").configure { dependsOn("prepareWebAssets") }
